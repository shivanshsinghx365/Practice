#include <iostream>
using namespace std;
int main(){
    while(20){
    cout<<"Hello";
    }
    //while(false) and while(0) are false and don't work even once until we are using do-while
    //while(true) and while(non-zero value) are considered true and run infinitely until there is some other way to exit it, example break;
    //non zero values are treated as true and 0 is treated as false
}